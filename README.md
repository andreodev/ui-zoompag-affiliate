
  # Painel de Afiliados

  This is a code bundle for Painel de Afiliados. The original project is available at https://www.figma.com/design/4gSJGB2tmpPdqWXF2iijGb/Painel-de-Afiliados.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  